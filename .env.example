# Chave do Google AI Studio (Gemini Flash) — free tier, sem cartão.
# Sem ela, o pipeline ainda roda usando a descrição do RSS como resumo (fallback).
GEMINI_API_KEY=

# Opcional: URL canônica do site (sitemap/canonical). Default no astro.config.
# SITE_URL=https://noticias.globalnote.com.br
