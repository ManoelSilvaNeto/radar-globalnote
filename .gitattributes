# Normaliza fim de linha: no repo tudo é LF; o checkout NÃO converte pra CRLF.
# Evita diffs/ruído ao clonar no Windows (a máquina nova do dono).
* text=auto eol=lf

# Binários — nunca normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
