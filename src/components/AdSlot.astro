---
// Espaço de anúncio RESERVADO. AdSense desativado no MVP — quando houver tráfego,
// é só plugar o script aqui. Renderiza vazio (sem ocupar altura) por enquanto.
const enabled = false;
---

{
  enabled && (
    <aside aria-hidden="true" class="my-6 flex min-h-24 items-center justify-center rounded-lg border border-dashed border-neutral-300 text-xs text-neutral-400 dark:border-neutral-700">
      publicidade
    </aside>
  )
}
